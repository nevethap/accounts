var expect = require('chai').expect,
	  app = require('../app/new-account-form/form-controller');

    describe('oneEqualsOne', function () {
      it('should return true when one equals one', function () {
        expect(1).to.equal(1);
      });
    });
